import React from 'react';
import { Component } from 'react';

const User = ({match, location}) => {

    console.log(match);
    console.log(location);
    return (
        <div>

        </div>
    )
}

export default User;